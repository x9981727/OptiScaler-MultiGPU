diff --git a/State.h b/State.h
index f206414..43e24ed 100644
--- a/State.h
+++ b/State.h
@@ -226,6 +226,10 @@ class State
     bool skipDxgiLoadChecks = false;
     bool skipParentWrapping = false;
 
+    // MultiGPU XeFG initialization isolation. Thread-local because XeFG may initialize on a game thread
+    // while unrelated DXGI/D3D12 work happens elsewhere.
+    inline static thread_local bool multiGpuXeFGInit = false;
+
     // quirks
     std::vector<std::string> detectedQuirks {};
 
@@ -406,6 +410,21 @@ class ScopedSkipDxgiLoadChecks
     ~ScopedSkipDxgiLoadChecks() { State::Instance().skipDxgiLoadChecks = previousState; }
 };
 
+class ScopedMultiGpuXeFGInit
+{
+  private:
+    bool previousState;
+
+  public:
+    ScopedMultiGpuXeFGInit()
+    {
+        previousState = State::multiGpuXeFGInit;
+        State::multiGpuXeFGInit = true;
+    }
+
+    ~ScopedMultiGpuXeFGInit() { State::multiGpuXeFGInit = previousState; }
+};
+
 class ScopedSkipParentWrapping
 {
   private:
diff --git a/framegen/xefg/XeFG_Dx12.cpp b/framegen/xefg/XeFG_Dx12.cpp
index 1e6ffb6..2343993 100644
--- a/framegen/xefg/XeFG_Dx12.cpp
+++ b/framegen/xefg/XeFG_Dx12.cpp
@@ -408,8 +408,18 @@ bool XeFG_Dx12::CreateSwapchain(IDXGIFactory* factory, ID3D12CommandQueue* cmdQu
 
     xefg_swapchain_result_t result;
     auto xefgQueue = SelectedFGQueue(realQueue);
-    result = XeFGProxy::D3D12InitFromSwapChainDesc()(_swapChainContext, hwnd, &scDesc, &fsDesc, xefgQueue, factory12,
-                                                     &params);
+    if (IsMultiGPUActive())
+    {
+        LOG_INFO("MultiGPU v7: isolating XeFG internal DXGI/D3D12 initialization");
+        ScopedMultiGpuXeFGInit isolateXeFGInit {};
+        result = XeFGProxy::D3D12InitFromSwapChainDesc()(_swapChainContext, hwnd, &scDesc, &fsDesc, xefgQueue,
+                                                         factory12, &params);
+    }
+    else
+    {
+        result = XeFGProxy::D3D12InitFromSwapChainDesc()(_swapChainContext, hwnd, &scDesc, &fsDesc, xefgQueue,
+                                                         factory12, &params);
+    }
 
     if (result != XEFG_SWAPCHAIN_RESULT_SUCCESS)
     {
@@ -576,8 +586,18 @@ bool XeFG_Dx12::CreateSwapchain1(IDXGIFactory* factory, ID3D12CommandQueue* cmdQ
 
     xefg_swapchain_result_t result;
     auto xefgQueue = SelectedFGQueue(realQueue);
-    result = XeFGProxy::D3D12InitFromSwapChainDesc()(_swapChainContext, hwnd, desc, pFullscreenDesc, xefgQueue,
-                                                     factory12, &params);
+    if (IsMultiGPUActive())
+    {
+        LOG_INFO("MultiGPU v7: isolating XeFG internal DXGI/D3D12 initialization");
+        ScopedMultiGpuXeFGInit isolateXeFGInit {};
+        result = XeFGProxy::D3D12InitFromSwapChainDesc()(_swapChainContext, hwnd, desc, pFullscreenDesc, xefgQueue,
+                                                         factory12, &params);
+    }
+    else
+    {
+        result = XeFGProxy::D3D12InitFromSwapChainDesc()(_swapChainContext, hwnd, desc, pFullscreenDesc, xefgQueue,
+                                                         factory12, &params);
+    }
 
     State::Instance().skipSpoofing = false;
 
diff --git a/hooks/D3D12_Hooks.cpp b/hooks/D3D12_Hooks.cpp
index 947ec8d..2c33cc9 100644
--- a/hooks/D3D12_Hooks.cpp
+++ b/hooks/D3D12_Hooks.cpp
@@ -1332,6 +1332,15 @@ static HRESULT hkD3D12CreateDevice(IUnknown* pAdapter, D3D_FEATURE_LEVEL Minimum
     LOG_DEBUG("Adapter: {:X}, Level: {:X}, Caller: {}", (size_t) pAdapter, (UINT) MinimumFeatureLevel,
               Util::WhoIsTheCaller(_ReturnAddress()));
 
+    if (State::multiGpuXeFGInit)
+    {
+        LOG_TRACE("MultiGPU v7: passthrough D3D12CreateDevice during XeFG internal initialization");
+        _creatingD3D12Device = true;
+        auto result = o_D3D12CreateDevice(pAdapter, MinimumFeatureLevel, riid, ppDevice);
+        _creatingD3D12Device = false;
+        return result;
+    }
+
 #ifdef ENABLE_DEBUG_LAYER_DX12
     LOG_WARN("Debug layers active!");
     if (debugController == nullptr && D3D12GetDebugInterface(IID_PPV_ARGS(&debugController)) == S_OK)
diff --git a/hooks/DxgiFactory_Hooks.cpp b/hooks/DxgiFactory_Hooks.cpp
index 025a238..9c520e2 100644
--- a/hooks/DxgiFactory_Hooks.cpp
+++ b/hooks/DxgiFactory_Hooks.cpp
@@ -173,6 +173,13 @@ HRESULT DxgiFactoryHooks::CreateSwapChain(IDXGIFactory* realFactory, IUnknown* p
         return res;
     }
 
+    if (State::multiGpuXeFGInit && _skipFGSwapChainCreation)
+    {
+        LOG_INFO("MultiGPU v7: passthrough XeFG internal CreateSwapChain without Opti wrapping");
+        ScopedSkipParentWrapping skipParentWrapping {};
+        return o_CreateSwapChain(realFactory, pDevice, pDesc, ppSwapChain);
+    }
+
     if (pDevice == nullptr || pDesc == nullptr)
     {
         LOG_WARN("pDevice or pDesc is nullptr!");
@@ -468,6 +475,14 @@ HRESULT DxgiFactoryHooks::CreateSwapChainForHwnd(IDXGIFactory2* realFactory, IUn
         return result;
     }
 
+    if (State::multiGpuXeFGInit && _skipFGSwapChainCreation)
+    {
+        LOG_INFO("MultiGPU v7: passthrough XeFG internal CreateSwapChainForHwnd without Opti wrapping");
+        ScopedSkipParentWrapping skipParentWrapping {};
+        return o_CreateSwapChainForHwnd(realFactory, pDevice, hWnd, pDesc, pFullscreenDesc, pRestrictToOutput,
+                                        ppSwapChain);
+    }
+
     if (pDevice == nullptr || pDesc == nullptr)
     {
         LOG_WARN("pDevice or pDesc is nullptr!");
diff --git a/hooks/DxgiFactory_WrappedCalls.cpp b/hooks/DxgiFactory_WrappedCalls.cpp
index bbec3dd..c7fbd6b 100644
--- a/hooks/DxgiFactory_WrappedCalls.cpp
+++ b/hooks/DxgiFactory_WrappedCalls.cpp
@@ -67,6 +67,13 @@ HRESULT DxgiFactoryWrappedCalls::CreateSwapChain(IDXGIFactory* realFactory, Wrap
         return realFactory->CreateSwapChain(pDevice, pDesc != nullptr ? &localDesc : nullptr, ppSwapChain);
     }
 
+    if (State::multiGpuXeFGInit && _skipFGSwapChainCreation)
+    {
+        LOG_INFO("MultiGPU v7: passthrough wrapped-factory XeFG internal CreateSwapChain without Opti wrapping");
+        ScopedSkipParentWrapping skipParentWrapping {};
+        return realFactory->CreateSwapChain(pDevice, pDesc, ppSwapChain);
+    }
+
     if (pDevice == nullptr || pDesc == nullptr)
     {
         LOG_WARN("pDevice or pDesc is nullptr!");
@@ -358,6 +365,14 @@ HRESULT DxgiFactoryWrappedCalls::CreateSwapChainForHwnd(IDXGIFactory2* realFacto
         return res;
     }
 
+    if (State::multiGpuXeFGInit && _skipFGSwapChainCreation)
+    {
+        LOG_INFO("MultiGPU v7: passthrough wrapped-factory XeFG internal CreateSwapChainForHwnd without Opti wrapping");
+        ScopedSkipParentWrapping skipParentWrapping {};
+        return realFactory->CreateSwapChainForHwnd(pDevice, hWnd, pDesc, pFullscreenDesc, pRestrictToOutput,
+                                                   ppSwapChain);
+    }
+
     if (pDevice == nullptr || pDesc == nullptr)
     {
         LOG_WARN("pDevice or pDesc is nullptr!");
